[pytest]
markers =
    unit: Unit tests with mocked dependencies (fast)
    integration: Integration tests with real AWS connections (slow)
    acceptance: End-to-end acceptance tests (slowest, requires AWS credentials)

testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
